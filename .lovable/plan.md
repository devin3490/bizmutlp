

## Plan

Supprimer le badge callout "Tu es un vendeur ou une vendeuse" dans `src/components/HeroSection.tsx` (lignes 88-98 environ — le bloc `motion.div` avec la classe `inline-block mb-6 px-5 py-2 rounded-full border...`).

